//Manipulação de eventos no React é semelhante a manipulação de eventos no DOM.
//Eventos no react são escritos em camelCase.
//Com o jsx você passa uma função ao inves de uma string.

/* 
    const showEvent = (e) => {  
        console.log('Evento disparado');
        console.log(e)
    }

    const Button = <button onClick={showEvent}>Mostrar Evento</button> 

    function app2 (){
        return (
            <div>
                <h1 id="algo">Algo</h1>
                {Button}
            </div>
        )
    }

    export default app2;
*/